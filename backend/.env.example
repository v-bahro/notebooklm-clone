PORT=3000
FRONTEND_ORIGIN=http://localhost:4200
DB_HOST=localhost
DB_PORT=5432
DB_USERNAME=notebooklm
DB_PASSWORD=notebooklm
DB_NAME=notebooklm

# Für den Chat (Phase 3): ohne diese beiden Keys funktionieren Notebooks/
# Quellen weiterhin normal, der Chat antwortet dann nur mit einer
# Fehlermeldung statt einer echten Antwort.
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
